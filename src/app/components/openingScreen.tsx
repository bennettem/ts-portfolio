import Animation from "./animation";

const OpeningScreen = () => {
  return (
    <div className="h-screen w-full bg-page">
      <Animation />
    </div>
  );
};

export default OpeningScreen;
